---
language: c++
dist: focal
os:
  linux
compiler:
  - clang
  - gcc
addons:
  apt:
    packages:
      - cmake
      - ninja-build
script:
  - cmake --preset gcc-release-ninja
  - cd build/gcc-release-ninja
  - ninja
  - ctest -V
  - sudo cpack .
